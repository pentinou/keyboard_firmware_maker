/*
 * Generated by keyboard_firmware_maker — RIGHT HALF
 * Keyboard: {{ display_name }}
 */

#include "{{ shield_name }}.dtsi"

&default_transform {
    col-offset = <{{ matrix_cols }}>;
};

&kscan0 {
    row-gpios = {{ row_gpios_right }}
        ;
    col-gpios = {{ col_gpios_right }}
        ;
};

{% if has_encoder %}
/* Active l'encodeur local de cette moitié uniquement. */
&right_encoder {
    status = "okay";
};
{% endif %}
